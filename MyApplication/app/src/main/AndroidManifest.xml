<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CreatingRoomTimeout"></activity>
        <activity android:name=".view.EndGameScreen" />
        <activity android:name=".view.VotingTimeoutActivity" />
        <activity android:name=".view.GameSessionNight" />
        <activity android:name=".view.GameSessionActivity" />
        <activity android:name=".view.GameLobbyActivity" />
        <activity android:name=".view.CreateRoomActivity" />
        <activity android:name=".view.JoinRoomActivity" />
        <activity android:name=".view.InstructionActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>